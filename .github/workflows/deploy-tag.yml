name: 'Deploy Tag'

on:
  push:
      branches:
        - main
jobs:
  if_merged:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    steps:
      - run: |
            echo The commit was pushed
        with:
          repository: 'Bluubi/reCAT'
